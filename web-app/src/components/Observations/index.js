import React from 'react';

import Title from '../Title';

const Observations = () => {
  return (
    <div>
      <Title name="Observations" />
      Observations
    </div>
  );
};

export default Observations;
